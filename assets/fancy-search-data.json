{
    {{- $first:= true}}
    {{- range $index, $page := where .Site.Pages "Kind" "in" (slice "home" "page" "section") }}
      {{- $i :=add $index 1 }}
      {{- if $page.Content }}
      {{- if not $first }}
      ,
      {{- end -}}
      "{{$i}}": {
        "url": "{{ $page.RelPermalink }}",
        "relUrl": "{{ $page.RelPermalink }}",
        "title": {{ (partial "docs/title" default "Title" $page) | jsonify }},
        "doc": {{ (partial "docs/title" default "Doc" $page) | jsonify }},
        "section": {{ (partial "docs/title" default "Section" $page.Parent) | jsonify }},
        "content": {{$page.Plain | jsonify}}
      }
      {{- $first = false}}
      {{- end -}}
    {{- end -}}
  }
  